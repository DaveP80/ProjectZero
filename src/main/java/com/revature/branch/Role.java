package com.revature.branch;

public enum Role {
     Admin,
     Employee,
     Customer
     
     //its important that the casing of these variables matches directly
     //to how theyre listed in the DB
}
